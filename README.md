# felib

Simple finite element equation solver

## Installation

## One time setup

```console
git clone git@github.com:tjfulle/felib
cd felib
python3 -m venv venv
source venv/bin/activate
python3 -m pip install -e .
```

## Run an example

```console
cd examples
python3 plate_with_hole.py
```
